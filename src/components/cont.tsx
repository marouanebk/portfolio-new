const Cont = ({ children }: any) => {
    return (
        <div className="mx-2 sm:mx-3 md:mx-12 xl:mx-20 px-2 min-h-screen overflow-hidden">
            {children}
        </div>
    );
};

export default Cont;
